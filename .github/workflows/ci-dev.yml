name: CI

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout commit 🛎️
        uses: actions/checkout@v2.4.0

      - name: Install Node ⬢
        uses: actions/setup-node@v3
        with:
          node-version: '16.x'
      
      - name: Install 🔧
        run: |
          npm install

      - name: Build 🧩
        run: |
          npm run build

      - name: Test 🧪
        run: |
          set -e
          npm test
